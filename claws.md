| Feature | **Pishkar** | **OpenClaw** | **nanoclaw** | **NemoClaw** | **TinyClaw** | **Mercury** | **IronClaw** | **microclaw** | **ZeptoClaw** | **PicoClaw** | **ApexClaw** | **Hermes** | **nanobot** | **MimiClaw** | **RT-Claw** | **KrillClaw** |
| ------- | ----------- | ------------ | ------------ | ------------ | ------------ | ----------- | ------------ | ------------- | ------------- | ------------ | ------------ | ---------- | ----------- | ------------ | ----------- | ------------- |
| Git URL | (this repo) | [openclaw/openclaw](https://github.com/openclaw/openclaw) | [qwibitai/nanoclaw](https://github.com/qwibitai/nanoclaw) | [NVIDIA/NemoClaw](https://github.com/NVIDIA/NemoClaw) | [jlia0/tinyclaw](https://github.com/jlia0/tinyclaw) | [cosmicstack-labs/mercury-agent](https://github.com/cosmicstack-labs/mercury-agent) | [nearai/ironclaw](https://github.com/nearai/ironclaw) | [microclaw/microclaw](https://github.com/microclaw/microclaw) | [qhkm/zeptoclaw](https://github.com/qhkm/zeptoclaw) | [sipeed/picoclaw](https://github.com/sipeed/picoclaw) | [AmarnathCJD/ApexClaw](https://github.com/AmarnathCJD/ApexClaw) | [nousresearch/hermes-agent](https://github.com/nousresearch/hermes-agent) | [HKUDS/nanobot](https://github.com/HKUDS/nanobot) | [memovai/mimiclaw](https://github.com/memovai/mimiclaw) | [zevorn/rt-claw](https://github.com/zevorn/rt-claw) | [krillclaw/KrillClaw](https://github.com/krillclaw/KrillClaw) |
| Language | Python | TypeScript | TypeScript | TypeScript (+ Shell, NVIDIA wrapper) | TypeScript (lightweight, often Claude-driven) | TypeScript | Rust | Rust | Rust | Go | Go | Python | Python | C (embedded, ESP32-S3) | C (embedded) | Zig (microcontroller) |
| ★ Stars | — | 364k | 28k | 19.8k | 3.5k | 817 | 12k | 664 | 611 | 28.5k | 29 | 116k | 40.8k | 5.3k | 692 | 175 |
| Execution pattern | **Hybrid** — deterministic Gateway wrapping a hand-rolled agent loop; serves both conversational and delegated modes | **Hybrid** — multi-channel conversational shell with an agent loop in the middle; Gateway is the deterministic backbone | **Conversational** — minimal chat shell, one channel, one loop, container per session | **Workflow** — YAML "blueprints" define routed inference; the most workflow-shaped of the set | **Hybrid (multi-agent)** — spawns Claude/Codex CLI subprocesses per workspace; `@mention` routing | **Hybrid (governed agent)** — agent loop wrapped in permission gates, token budget, daemon — workflow-like controls around agent freedom | **Autonomous agent** — production-grade chat agent with WASM sandbox + prompt-injection detection | **Autonomous agent** — unified loop with a background *reflector* that extracts and dedupes durable facts | **Autonomous agent** — compact production loop; SHA256 loop detection, tiered compaction | **Hybrid** — agent loop with explicit workflow primitives (Hooks, Steering, SubTurn) bolted on | **Autonomous agent** — always-on Telegram agent with 100+ tools incl. browser automation | **Autonomous agent (self-improving)** — RL skill loop, FTS5 search, 6 terminal backends | **Conversational** — small-and-readable core, MCP-ready, "2 steps to add a provider" | **Autonomous agent (self-scheduling)** — `cron.json` + `HEARTBEAT.md` — agent edits its own task list | **Hybrid (swarm)** — embedded distributed agents; capability bitmap for cross-node tool invocation | **Conversational** — embedded chat over BLE/Serial/HTTP; priority-based context truncation |
| Agent loop implementation | **Hand-rolled** `run_turn` with Hooks / Steering / SubTurn primitives, SHA256 loop guard, tool-aware compaction, max-turn budget = 10 | Hand-rolled | Hand-rolled | Hand-rolled (custom NVIDIA wrapper) | **CLI subprocess** — shells out to `claude`/`codex` per turn | **SDK as engine** — Vercel AI SDK v4 (`generateText` / `streamText`) | Hand-rolled | Hand-rolled | Hand-rolled | Hand-rolled (Hooks/Steering require it) | Hand-rolled | Hand-rolled (RL training requires loop access) | Hand-rolled | Hand-rolled | Hand-rolled | Hand-rolled (incl. hand-rolled JSON) |
| Memory & state | **Workspace markdown + SQLite session log**; `~/.pishkar/users/<user_id>/{SOUL,AGENTS,USER,HEARTBEAT}.md`; SQLite append-only with `user_id`/`session_id` columns | Workspace markdown only (`SOUL.md` / `AGENTS.md` / `TOOLS.md` / `skills/`) | Workspace + SQLite session log | YAML blueprints + SQLite per Docker session | Workspace + SQLite WAL queue (per-agent workspaces) | Workspace + **10 typed memory categories** w/ confidence/importance/durability + auto-consolidation every 60 min | **PostgreSQL + pgvector** w/ Reciprocal Rank Fusion semantic search | SQLite + **sqlite-vec** semantic memory + background reflector | SQLite + tiered context compaction (70 / 90 / 95% thresholds) | SQLite + rule-based model routing per task | SQLite (Telegram-bound; chat_id keyed) | SQLite + **FTS5** cross-session search + RL skill memory | Workspace + SQLite (small readable core) | Workspace markdown only (`HEARTBEAT.md` + `cron.json` — agent self-edits) | In-memory (embedded) + swarm capability bitmap | In-memory + priority-based context truncation (no DB) |
| Channels (UI surfaces) | **FastAPI + WebSocket** server; Web UI + CLI as WS client (day 1); typed event protocol (`turn_start` / `message_start` / `content_block_*` / `tool_result` / `turn_end`); pydantic→TS codegen | **20+ channels** (Web UI, CLI, Telegram, Slack, …) via Gateway | CLI + WS (channel registry self-registers at startup) | CLI + hardened Docker sandboxes (Landlock + seccomp + netns) | Multi-agent CLI workspaces; `@mention` routing | Web UI + CLI + **OS-native daemon** (LaunchAgent / systemd / Task Scheduler) | CLI + SSE / POST server | Per-chat tool authorization across multiple platform adapters | CLI (~6 MB binary, hot-config reload) | CLI + multi-channel (Hooks fire on channel events) | **Telegram-native** (Maton API to skip OAuth complexity) | **6 terminal backends:** local / Docker / SSH / Daytona / Modal / Singularity | Many channels (small core abstraction); WebSocket-first | ESP32-S3 hardware (BLE / Wi-Fi); self-schedules | Embedded swarm (FreeRTOS / RT-Thread / Linux); OSAL | **Vtable transports:** HTTP / BLE / Serial |
| Safety & governance | **Mercury-equivalent**: approval gate (*Ask Me / Allow Once / Allow All Session*) + `BudgetedProvider` (auto-concise @ 70%) + loop guard + max-turn + per-tool timeout + max-result cap; folder scopes deferred | Gateway-mediated; per-channel trust | Minimum (loop guard only; single-user) | **Sandbox-as-governance** (Landlock + seccomp + netns) | Per-agent workspace isolation | **Approval gate (Ask Me / Allow Once / Allow All) + token budget (auto-concise at 70%) + folder scopes + daemon recovery** | **WASM sandbox + prompt-injection detection** | Per-chat tool authorization (lightweight permission gate) | Loop guard (SHA256) + tiered compaction (70/90/95%) | Hooks/Steering enable governance; rule-based model routing | Telegram-bound `chat_id` permissions | **Sandbox-as-governance** (Docker / SSH / Daytona / Modal / Singularity isolation) | Minimum (small readable core) | Minimum (embedded; NVS-flash secrets split) | Swarm capability bitmap (per-node authorization) | Minimum (embedded; priority-based truncation) |
| Tool extensibility | **Native `@tool` decorator + `mcp_bridge`** (stdio + HTTP-stream MCP servers); day-one tools: `bash`, `read_file`, `write_file`, `http` | Native `@tool`; Gateway-mediated registry | Native `@tool` only | Native `@tool` + YAML blueprints (workflow-shaped) | Tools delegated to CLI subprocesses (Claude / Codex) per agent | Native `@tool` + permission gates, folder scopes | Native `@tool` in WASM sandbox | **Native `@tool` + MCP bridge**; per-chat tool auth | Native `@tool` (compact) | **Native `@tool` + MCP bridge**; Hooks route tools | Native `@tool` (100+ tools incl. browser automation; Maton OAuth shortcut) | Native `@tool` + RL-tracked skill registry across 6 backends | **Native `@tool` + MCP bridge** (MCP-ready core) | Native `@tool` (embedded, cron-driven) | Native `@tool` + swarm capability bitmap | Native `@tool` (embedded, hand-rolled JSON) |
| Observability & tracing | **Local SQLite append-only log + LangFuse self-hosted** (Docker); fail-open Hook exporter via `asyncio.create_task`; data sovereignty preserved | Local SQLite log + workspace markdown audit (Gateway-level events) + OpenTelemetry tracing | Local SQLite session log only | Per-Docker-session logs + YAML blueprint audit | SQLite WAL queue + CLI subprocess stdout/stderr capture | SQLite + permission-decision audit + token-budget metrics | PostgreSQL log + prompt-injection audit | SQLite + reflector extraction audit (dedupe trail) | Prometheus + JSON metrics export, structured logging, per-tenant tracing, SQLite + compaction audit (per-threshold) | **Hooks layer** (`before_tool` / `after_llm`) is the documented observability seam, configurable `gateway.log_level` | SQLite (chat_id keyed); Telegram message audit + rotating file logs + `debug_trace` execution profiler | SQLite + FTS5 search + RL training reward logs | SQLite (small readable core); WebSocket event stream visible to client + **Langfuse observability** | Serial console + `HEARTBEAT.md` self-edits as audit trail | In-memory ring buffer (embedded; lossy) | Serial console only; in-memory (no persistence), macOS simulator command logging |
| Model provider strategy | **LiteLLM library + `Router` failover** (Anthropic primary, OpenAI fallback, "remember last successful"); 100+ providers via config; built-in LangFuse callbacks | Hand-rolled multi-provider via Gateway abstraction | Hand-rolled, single provider (TS) | NVIDIA-routed inference (Triton / NIM-style); YAML blueprints select model | Multi-provider via CLI subprocess (Claude CLI / Codex CLI) | **Vercel AI SDK v4** — multi-provider for free; "remember last successful" failover | Hand-rolled provider abstraction (Rust) | Hand-rolled provider abstraction (Rust) | Hand-rolled provider abstraction (Rust) | Hand-rolled + **rule-based model routing per task** | Hand-rolled provider abstraction (Go) | Hand-rolled across 6 backends | Hand-rolled abstraction; *"2 steps to add a provider"* | Single provider (embedded; on-device or single API) | Single provider per swarm node | Single provider (embedded, hand-rolled JSON) |
| Multi-user identity | **Reserve seams** (`InboundMessage.user_id`, per-user workspace path, per-user budget + approval scoping); single-user UX day one; per-channel auth + shared/private facts deferred | Multi-channel implies multi-context; no documented per-user identity | Single-user implicit | Per-Docker-session isolation | Per-agent workspaces (multi-agent ≠ multi-user) | Single-user daemon (folder scopes are per-user-ish) | Single-user implicit | **Per-chat tool authorization** (lightweight multi-user) | **Per-tenant tracing** (closest to real multi-tenancy in the set) | Multi-channel; no documented per-user | **Telegram `chat_id` keyed** (lightweight multi-user) | Multi-backend isolation (per-backend, not per-user) | Single-user implicit (small core) | Single-user (one device) | Per-node in swarm (not really multi-user) | Single-user (embedded) |
| Proactive triggers | **`TriggerSource(Protocol)` abstraction + `HeartbeatTrigger`** (cheap-tick: file I/O on `HEARTBEAT.md` / `cron.json`, LLM only when due); future: `WebhookTrigger`, `HomeAssistantTrigger`, `MQTTTrigger`, `EmailTrigger` as additive impls | **Multi-channel reactive + Heartbeat + Cron.** Uses `HEARTBEAT.md` for periodic "patrols" and `cron.json` for exact-time tasks. | Reactive only (channel polling) | Workflow-triggered (YAML blueprints route inference by input shape) | Reactive (CLI/agent-driven) | Reactive + OS-daemon recovery (no proactive triggers) | Reactive | Reactive | Reactive | Reactive; Hooks fire on loop events but Hooks ≠ external triggers | Reactive (Telegram-driven) | **Timer-based:** structured `scheduled_tasks/` directory | Reactive | **`HEARTBEAT.md` + `cron.json`** — canonical timer-trigger pattern; agent self-edits | **Sensor / capability triggers** via swarm capability bitmap (closest to HA-style sensory triggers in the set) | Reactive (embedded chat) |
| Tool sandboxing | **`SubprocessToolRunner` + reserved `ToolRunner(Protocol)` seam** (Docker / WASM / Landlock as configuration-only later swaps); approval gate covers the threat model day one | **Docker per session** (default for non-main sessions) | Container per session (Docker) | **Hardened Docker + Landlock + seccomp + netns** | Per-agent CLI subprocess workspaces | None (relies on approval gate + folder scopes) | **WASM tool sandboxing** (capability-based) | None (per-chat tool authorization is permission-based, not isolation) | None documented; relies on loop guard | None documented; relies on Hooks/Steering | None (Telegram-bound; trusts user) | **6 isolation tiers** (local / Docker / SSH / Daytona / Modal / Singularity) | None (small core) | NVS-flash secrets isolation (embedded; weak sandbox) | OSAL isolation (per-node in swarm) | None (microcontroller, single binary) |
| Skill system | **Workspace markdown skills, Anthropic `SKILL.md` format** (frontmatter `name` / `description` / optional `model`); compatible with Claude Code / Cowork tooling; embedding-search discovery deferred until > 10 skills | **Workspace markdown** (`skills/<name>/SKILL.md`) | None | YAML blueprints (workflow-shaped, not skills) | Per-agent workspaces; each agent has its own skills | None documented (governance-focused core) | None documented | None documented | None documented | **Workspace markdown skills** + Hooks/Steering primitives | None (100+ hardcoded tools, not skills) | **Self-improving skill registry** (RL-tracked; agent writes new SKILL.md after tasks; edits on errors) | None (small core) | Workspace markdown (limited; embedded) | Capability bitmap (swarm-level skills) | None (embedded) |
| Eval & testing | **Pytest unit + integration with `MockLiteLLMProvider`**; SQLite schema supports replay; `tests/replay/` reserved for once sessions accumulate; LLM-as-judge deferred | Not detailed in research; large project implies standard test suite | Not documented | Sandbox correctness tests (Landlock / seccomp) | Not documented | Permission-decision audit (governance correctness) | WASM sandbox correctness + prompt-injection detection eval | Reflector dedupe correctness | Loop-detector + tiered compaction correctness | Hook / Steering primitive tests | Not documented | **RL training reward signals** (implicit eval through self-improvement) | Not documented (small core) | Not documented (embedded) | Not documented (embedded) | macOS simulator command logging (debugging aid; not formal eval) |
| Backup & sync | **Deferred** (~3–4 weeks in); workspace planned to live in a local git repo by convention; OS-level backup (Time Machine etc.) covers the gap | Workspace markdown is git-friendly (version-controllable by default); no documented external backup | Not documented | Per-Docker-session ephemeral; no documented backup | Per-agent SQLite WAL queue; not documented | Daemon mode + crash recovery; no documented external backup | PostgreSQL — standard DB backup tooling applies | SQLite + sqlite-vec; not documented | Compact binary + SQLite; hot-config reload; not documented | Workspace markdown is git-friendly | Per-chat SQLite (Telegram-bound); not documented | Workspace markdown + RL skill memory; git-friendly | SQLite; not documented | Workspace markdown self-edited (git-friendly) | In-memory; ephemeral (no persistence) | In-memory; ephemeral (no persistence) |
| Runtime deployment | **Manual `python -m pishkar.server`** day one; one stable entrypoint; OS-native daemon (LaunchAgent / systemd / Task Scheduler) seam reserved for week 2 | Local-first Gateway (manual launch) | Manual (one process) | Docker per session | Manual; spawns CLI subprocesses per agent | **OS-native daemon** (LaunchAgent / systemd / Task Scheduler) + crash recovery + exponential backoff | Manual; CLI / SSE+POST server | Manual | Manual; ~6 MB compact binary; hot-config reload | Manual | **Always-on Telegram bot** (deployed wherever it can hold a persistent connection) | **Multi-backend**: local / Docker / SSH / Daytona / Modal / Singularity | Manual | **Embedded firmware** (ESP32-S3, always-on) | **Embedded firmware** (FreeRTOS / RT-Thread / Linux) | **Embedded firmware** (microcontroller) |
| Resilience | **Append-only log + SQLite-backed queue + transactional tool-result commit (UUID per call) + mid-turn crash detection on startup + trigger catch-up (`was_due_at`) + channel reconnection with `last_event_id` replay**; fail-open Hooks; tool-aware compaction; loop guard; LiteLLM Router failover; atomic workspace writes | Local-first Gateway with persistent channel state | **SQLite-backed queue** (Channels → SQLite → polling loop) — resumes from where it left off | Docker per session — crashed session does not take down others | **SQLite WAL queue** per-agent workspace (each agent state independent) | **OS-daemon supervision** (LaunchAgent / systemd / Task Scheduler) + crash recovery + exponential backoff | WASM sandboxing limits crash blast radius; PostgreSQL durability | Reflector dedupe handles duplicate facts after partial recovery | **SHA256 loop detection** + **tiered compaction (70/90/95%)** + **hot-config reload** | Hooks fire on loop events (recovery hooks attachable); rule-based routing for task-level failover | Telegram channel persistence; chat_id-keyed state recovery | **6 backends** (local / Docker / SSH / Daytona / Modal / Singularity) — partial failure ≠ total failure | Workspace + SQLite (small core); standard Python resilience | **`HEARTBEAT.md` self-edits in plain text** — survives any reboot by construction; ESP32 reboot-tolerant | **Embedded swarm with capability bitmap** — node failover | **Priority-based context truncation** (graceful degradation) |
