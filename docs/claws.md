| Feature | **Pishkar** | **OpenClaw** | **nanoclaw** | **NemoClaw** | **TinyClaw** | **Mercury** | **IronClaw** | **microclaw** | **ZeptoClaw** | **PicoClaw** | **ApexClaw** | **Hermes** | **nanobot** | **MimiClaw** | **RT-Claw** | **KrillClaw** |
| ------- | ----------- | ------------ | ------------ | ------------ | ------------ | ----------- | ------------ | ------------- | ------------- | ------------ | ------------ | ---------- | ----------- | ------------ | ----------- | ------------- |
| Git URL | (this repo) | [openclaw/openclaw](https://github.com/openclaw/openclaw) | [qwibitai/nanoclaw](https://github.com/qwibitai/nanoclaw) | [NVIDIA/NemoClaw](https://github.com/NVIDIA/NemoClaw) | [jlia0/tinyclaw](https://github.com/jlia0/tinyclaw) *(renamed to TinyAGI)* | [cosmicstack-labs/mercury-agent](https://github.com/cosmicstack-labs/mercury-agent) | [nearai/ironclaw](https://github.com/nearai/ironclaw) | [microclaw/microclaw](https://github.com/microclaw/microclaw) | [qhkm/zeptoclaw](https://github.com/qhkm/zeptoclaw) | [sipeed/picoclaw](https://github.com/sipeed/picoclaw) | [AmarnathCJD/ApexClaw](https://github.com/AmarnathCJD/ApexClaw) | [nousresearch/hermes-agent](https://github.com/nousresearch/hermes-agent) | [HKUDS/nanobot](https://github.com/HKUDS/nanobot) | [memovai/mimiclaw](https://github.com/memovai/mimiclaw) | [zevorn/rt-claw](https://github.com/zevorn/rt-claw) | [krillclaw/KrillClaw](https://github.com/krillclaw/KrillClaw) |
| Language | Python | TypeScript | TypeScript | TypeScript (+ Shell, NVIDIA wrapper) | TypeScript (lightweight, often Claude-driven) | TypeScript | Rust | Rust | Rust | Go | Go | Python | Python | C (embedded, ESP32-S3) | C (embedded) | Zig (microcontroller) |
| ★ Stars | — | 364k | 28k | 19.8k | 3.5k | 1.5k | 12k | 664 | 611 | 28.5k | 29 | 120k | 40.8k | 5.3k | 692 | 175 |
| Execution pattern | **Reactive Loop, Goal-Driven Autonomous** — deterministic Gateway wrapping a hand-rolled agent loop; serves both conversational and delegated modes | **Reactive Loop, Goal-Driven Autonomous** — multi-channel conversational shell with an agent loop in the middle; Gateway is the deterministic backbone | **Reactive Loop, Goal-Driven Autonomous** — channel registry + per-session Docker container + per-agent workspace + scheduled tasks + skills system; deterministic infrastructure wrapping the agent loop | **Workflow-Routed** — YAML "blueprints" define routed inference; the most workflow-shaped of the set | **Plan & Execute** — spawns Claude/Codex CLI subprocesses per workspace; `@mention` routing | **Goal-Driven Autonomous** — agent loop wrapped in permission gates, token budget, daemon — workflow-like controls around agent freedom | **Goal-Driven Autonomous** — production-grade chat agent with parallel jobs execution | **Plan & Execute** — unified loop with a background *reflector* that extracts and dedupes durable facts | **Goal-Driven Autonomous, Swarm / Distributed** — compact production loop with delegated sub-agents | **Reactive Loop, Goal-Driven Autonomous** — agent loop with explicit workflow primitives (Hooks, Steering, SubTurn) bolted on | **Goal-Driven Autonomous** — loops until the task is done; Telegram-native | **Plan & Execute** — RL skill loop, FTS5 search, isolated subagents for parallel workstreams | **Reactive Loop** — small-and-readable core, MCP-ready, "2 steps to add a provider" | **Goal-Driven Autonomous** — `cron.json` + `HEARTBEAT.md` — agent edits its own task list | **Swarm / Distributed** — embedded distributed agents; remote tool invocation across nodes | **Reactive Loop** — embedded chat over BLE/Serial/HTTP; priority-based context truncation |
| Agent loop implementation | **Hand-rolled** `run_turn` with Hooks / Steering / SubTurn primitives, SHA256 loop guard, tool-aware compaction, max-turn budget = 10 | Hand-rolled (Interactive Canvas / Interruptible) | **SDK as engine** — Anthropic Claude Agent SDK | Hand-rolled (custom NVIDIA wrapper) | **CLI subprocess** — shells out to `claude`/`codex` per turn | **SDK as engine** — Vercel AI SDK v4 (`generateText` / `streamText`) | Hand-rolled | Hand-rolled (Mid-conversation messaging / Streaming updates) | Hand-rolled | Hand-rolled (Hooks/Steering for mid-run injection) | Hand-rolled | Hand-rolled (interrupt-and-redirect) | Hand-rolled | Hand-rolled | Hand-rolled | Hand-rolled (incl. hand-rolled JSON) |
| Memory & state | **File-based Workspace, Append-only Database** — **Workspace markdown + SQLite session log**; `~/.pishkar/users/<user_id>/{SOUL,AGENTS,USER,HEARTBEAT}.md`; SQLite append-only with `user_id`/`session_id` columns | **File-based Workspace** — Workspace markdown only (`SOUL.md` / `AGENTS.md` / `TOOLS.md` / `skills/`) | **Append-only Database** — Per-agent `CLAUDE.md` + `groups/<folder>/` workspace + `inbound.db` / `outbound.db` per session (two SQLite files, one writer each — no cross-mount contention) | **Append-only Database** — YAML blueprints + SQLite per Docker session | **Append-only Database** — Workspace + SQLite WAL queue (per-agent workspaces) | **Typed Knowledge Graph, Active Context Compaction** — Workspace + **10 typed memory categories** + auto-consolidation + token budget enforcement | **Semantic Vector Recall** — **PostgreSQL + pgvector** w/ Hybrid RRF Search + Identity Files | **Semantic Vector Recall** — SQLite + **sqlite-vec** semantic memory + Temporal Knowledge Graph | **Active Context Compaction** — SQLite + tiered context compaction (70 / 90 / 95% thresholds) | **Append-only Database** — SQLite + rule-based model routing per task | **Semantic Vector Recall** — SQLite + **TF-IDF memory ranking** | **Semantic Vector Recall** — SQLite + **FTS5** cross-session search + Honcho dialectic user modeling | **Active Context Compaction** — Workspace + SQLite (Dream two-stage memory + context compact shrinks sessions) | **File-based Workspace** — Workspace markdown only on SPIFFS flash | **Append-only Database** — Short-term RAM ring buffer + long-term NVS Flash | **Active Context Compaction** — Persistent KV store + constant-memory FNV-1a loop detection |
| Channels (UI surfaces) | **FastAPI + WebSocket** server; Web UI + CLI as WS client (day 1); typed event protocol (`turn_start` / `message_start` / `content_block_*` / `tool_result` / `turn_end`); pydantic→TS codegen | **20+ channels** (Web UI, CLI, Telegram, Slack, …) via Gateway | **Multi-channel** (WhatsApp · Telegram · Slack · Discord · Gmail; channel registry self-registers at startup) | CLI + hardened Docker sandboxes (Landlock + seccomp + netns) | **Multi-team + multi-channel** CLI workspaces; `@mention` routing | Web UI + CLI + **OS-native daemon** (LaunchAgent / systemd / Task Scheduler) | CLI + SSE / POST server | Per-chat tool authorization across multiple platform adapters | CLI (~6 MB binary, hot-config reload) | CLI + WebUI Launcher / Setup Wizard + 18+ platforms | **Telegram-native** + Web Dashboard with JWT auth | **6 terminal backends:** local / Docker / SSH / Daytona / Modal / Singularity | Many channels + WebUI setup server | UART Serial REPL + Telegram | Chat-first UART Shell + Web Flash | **Vtable transports:** HTTP / BLE / Serial |
| Safety & governance | **Human-in-the-loop Gates** — **Mercury-equivalent**: approval gate (*Ask Me / Allow Once / Allow All Session*) + `BudgetedProvider` (auto-concise @ 70%) + loop guard + max-turn + per-tool timeout + max-result cap; folder scopes deferred | **Human-in-the-loop Gates, Container Isolation** — Gateway-mediated; per-channel trust | **Container Isolation** — **OneCLI credential vault** (credentials never enter container; per-agent policies + rate limits at proxy) + Docker per-agent + per-agent mounts + three-level isolation model | **Process/Kernel Sandboxing** — **Sandbox-as-governance** (Landlock + seccomp + netns) | **Human-in-the-loop Gates** — Per-agent workspace isolation | **Human-in-the-loop Gates** — **Approval gate (Ask Me / Allow Once / Allow All) + token budget (auto-concise at 70%) + folder scopes + daemon recovery** | **Capability-Limited Runtimes** — **WASM Sandbox** with capability-based permissions, leak detection, endpoint allowlisting | **Human-in-the-loop Gates** — Per-chat tool authorization (lightweight permission gate) | **Process/Kernel Sandboxing** — Loop guard (SHA256) + 6 Sandbox Runtimes + Aho-Corasick prompt injection detection + SSRF guards | **Human-in-the-loop Gates** — Hooks/Steering enable governance; rule-based model routing | **Operator Trust** — Telegram-bound `chat_id` permissions | **Container Isolation** — **Sandbox-as-governance** (Docker / SSH / Daytona / Modal / Singularity isolation) | **Operator Trust** — Minimum (small readable core) | **Operator Trust** — Minimum (embedded; NVS-flash secrets split) | **Operator Trust** — Swarm capability bitmap (per-node authorization) | **Capability-Limited Runtimes** — Compile-time profiles to strip unneeded tools natively |
| Tool extensibility | **Native Code Plugins, MCP Integration** — **Native `@tool` decorator + `mcp_bridge`** (stdio + HTTP-stream MCP servers); day-one tools: `bash`, `read_file`, `write_file`, `http` | **Native Code Plugins** — Native `@tool`; Gateway-mediated registry | **Native Code Plugins, MCP Integration** — Anthropic Claude Agent SDK + MCP tools + **skills-via-branches** (`/add-<channel>` / `/add-<provider>` copy modules into the fork on demand) | **Native Code Plugins** — Native `@tool` + YAML blueprints (workflow-shaped) | **Native Code Plugins** — Tools delegated to CLI subprocesses (Claude / Codex) per agent | **Native Code Plugins** — Native `@tool` + permission gates, folder scopes | **Dynamic Sandboxed Modules** — Native `@tool` in WASM sandbox | **Native Code Plugins, MCP Integration** — **Native `@tool` + MCP bridge**; per-chat tool auth | **Native Code Plugins** — Native `@tool` (compact) | **Native Code Plugins, MCP Integration** — **Native `@tool` + MCP bridge**; Hooks route tools | **Native Code Plugins** — Native `@tool` (100+ tools incl. browser automation; Maton OAuth shortcut) | **Self-Improving Skills** — Native `@tool` + RL-tracked skill registry across 6 backends | **Native Code Plugins, MCP Integration** — **Native `@tool` + MCP bridge** (MCP-ready core) | **Native Code Plugins** — Native `@tool` (embedded, cron-driven) | **Native Code Plugins** — Native `@tool` + swarm capability bitmap | **Native Code Plugins** — Native `@tool` (embedded, hand-rolled JSON) |
| Observability & tracing | **Local SQLite append-only log + LangFuse self-hosted** (Docker); fail-open Hook exporter via `asyncio.create_task`; data sovereignty preserved | Local SQLite log + workspace markdown audit (Gateway-level events) + OpenTelemetry tracing | Local SQLite session log only | Per-Docker-session logs + YAML blueprint audit | SQLite WAL queue + CLI subprocess stdout/stderr capture | SQLite + permission-decision audit + token-budget metrics | PostgreSQL log + prompt-injection audit | SQLite + reflector extraction audit (dedupe trail) | Prometheus + JSON metrics export, structured logging, per-tenant tracing, SQLite + compaction audit (per-threshold) | **Hooks layer** (`before_tool` / `after_llm`) is the documented observability seam, configurable `gateway.log_level` | SQLite (chat_id keyed); Telegram message audit + rotating file logs + `debug_trace` execution profiler | SQLite + FTS5 search + RL training reward logs | SQLite (small readable core); WebSocket event stream visible to client + **Langfuse observability** | Serial console + `HEARTBEAT.md` self-edits as audit trail | In-memory ring buffer (embedded; lossy) | Serial console only; in-memory (no persistence), macOS simulator command logging |
| Model provider strategy | **LiteLLM library + `Router` failover** (Anthropic primary, OpenAI fallback, "remember last successful"); 100+ providers via config; built-in LangFuse callbacks | Hand-rolled multi-provider via Gateway abstraction | **Anthropic Claude Agent SDK** natively + `/add-codex` (OpenAI Codex) + `/add-opencode` (OpenRouter / Google / DeepSeek) + `/add-ollama-provider` (local open-weight); multi-provider via skills, configurable per agent group | NVIDIA-routed inference (Triton / NIM-style); YAML blueprints select model | Multi-provider via CLI subprocess (Claude CLI / Codex CLI) | **Vercel AI SDK v4** — multi-provider for free; "remember last successful" failover | Hand-rolled provider abstraction (Rust) | Hand-rolled provider abstraction (Rust) | Hand-rolled provider abstraction (Rust) | Hand-rolled + **rule-based model routing per task** | Hand-rolled provider abstraction (Go) | Hand-rolled across 6 backends | Hand-rolled abstraction; *"2 steps to add a provider"* | Single provider (embedded; on-device or single API) | Single provider per swarm node | Single provider (embedded, hand-rolled JSON) |
| Multi-user identity | **Reserve seams** (`InboundMessage.user_id`, per-user workspace path, per-user budget + approval scoping); single-user UX day one; per-channel auth + shared/private facts deferred | Multi-channel implies multi-context; no documented per-user identity | **Three-level flexible isolation** — per-channel agent (full privacy), shared agent across channels (unified memory, separate conversations), or shared session across channels (one conversation across many surfaces); configurable per-channel via `/manage-channels` | Per-Docker-session isolation | Per-agent workspaces (multi-agent ≠ multi-user) | Single-user daemon (folder scopes are per-user-ish) | Single-user implicit | **Per-chat tool authorization** (lightweight multi-user) | **Per-tenant tracing** (closest to real multi-tenancy in the set) | Multi-channel; no documented per-user | **Telegram `chat_id` keyed** (lightweight multi-user) | Multi-backend isolation (per-backend, not per-user) | Single-user implicit (small core) | Single-user (one device) | Per-node in swarm (not really multi-user) | Single-user (embedded) |
| Proactive triggers | **`TriggerSource(Protocol)` abstraction + `HeartbeatTrigger`** (cheap-tick: file I/O on `HEARTBEAT.md` / `cron.json`, LLM only when due); future: `WebhookTrigger`, `HomeAssistantTrigger`, `MQTTTrigger`, `EmailTrigger` as additive impls | **Multi-channel reactive + Heartbeat + Cron.** Uses `HEARTBEAT.md` for periodic "patrols" and `cron.json` for exact-time tasks. | **Reactive + scheduled jobs** | Workflow-triggered (YAML blueprints route inference by input shape) | Reactive (CLI/agent-driven) | Reactive + OS-daemon recovery (no proactive triggers) | Reactive | Reactive | Reactive | Reactive; Hooks fire on loop events but Hooks ≠ external triggers | Reactive (Telegram-driven) | **Timer-based:** structured `scheduled_tasks/` directory | Reactive | **`HEARTBEAT.md` + `cron.json`** — canonical timer-trigger pattern; agent self-edits | **Sensor / capability triggers** via swarm capability bitmap (closest to HA-style sensory triggers in the set) | Reactive (embedded chat) |
| Tool sandboxing | **`SubprocessToolRunner` + reserved `ToolRunner(Protocol)` seam** (Docker / WASM / Landlock as configuration-only later swaps); approval gate covers the threat model day one | **Docker per session** (default for non-main sessions) | **Docker per-agent** (default) + optional **Docker Sandboxes micro-VM** isolation + Apple Container macOS-native opt-in | **Hardened Docker + Landlock + seccomp + netns** | Per-agent CLI subprocess workspaces | None (relies on approval gate + folder scopes) | **WASM tool sandboxing** (capability-based) | None (per-chat tool authorization is permission-based, not isolation) | None documented; relies on loop guard | None documented; relies on Hooks/Steering | None (Telegram-bound; trusts user) | **6 isolation tiers** (local / Docker / SSH / Daytona / Modal / Singularity) | None (small core) | NVS-flash secrets isolation (embedded; weak sandbox) | OSAL isolation (per-node in swarm) | None (microcontroller, single binary) |
| Skill system | **Workspace markdown skills, Anthropic `SKILL.md` format** (frontmatter `name` / `description` / optional `model`); compatible with Claude Code / Cowork tooling; embedding-search discovery deferred until > 10 skills | **Workspace markdown** (`skills/<name>/SKILL.md`) | **Skills via branches** (`/add-<name>` skill copies modules into the fork on demand) — "skills as feature flags" pattern; distinct from Anthropic SKILL.md format | YAML blueprints (workflow-shaped, not skills) | Per-agent workspaces; each agent has its own skills | None documented (governance-focused core) | None documented | None documented | None documented | **Workspace markdown skills** + Hooks/Steering primitives | None (100+ hardcoded tools, not skills) | **Self-improving skill registry** (RL-tracked; agent writes new SKILL.md after tasks; edits on errors) | None (small core) | Workspace markdown (limited; embedded) | Capability bitmap (swarm-level skills) | None (embedded) |
| Eval & testing | **Pytest unit + integration with `MockLiteLLMProvider`**; SQLite schema supports replay; `tests/replay/` reserved for once sessions accumulate; LLM-as-judge deferred | Not detailed in research; large project implies standard test suite | Not documented | Sandbox correctness tests (Landlock / seccomp) | Not documented | Permission-decision audit (governance correctness) | WASM sandbox correctness + prompt-injection detection eval | Reflector dedupe correctness | Loop-detector + tiered compaction correctness | Hook / Steering primitive tests | Not documented | **RL training reward signals** (implicit eval through self-improvement) | Not documented (small core) | Not documented (embedded) | Not documented (embedded) | macOS simulator command logging (debugging aid; not formal eval) |
| Backup & sync | **Deferred** (~3–4 weeks in); workspace planned to live in a local git repo by convention; OS-level backup (Time Machine etc.) covers the gap | Workspace markdown is git-friendly (version-controllable by default); no documented external backup | Not documented | Per-Docker-session ephemeral; no documented backup | Per-agent SQLite WAL queue; not documented | Daemon mode + crash recovery; no documented external backup | PostgreSQL — standard DB backup tooling applies | SQLite + sqlite-vec; not documented | Compact binary + SQLite; hot-config reload; not documented | Workspace markdown is git-friendly | Per-chat SQLite (Telegram-bound); not documented | Workspace markdown + RL skill memory; git-friendly | SQLite; not documented | Workspace markdown self-edited (git-friendly) | In-memory; ephemeral (no persistence) | In-memory; ephemeral (no persistence) |
| Runtime deployment & Footprint | **Heavyweight Runtime** — **Manual `python -m pishkar.server`** day one; one stable entrypoint; OS-native daemon (LaunchAgent / systemd / Task Scheduler) seam reserved for week 2 | **Heavyweight Runtime** — Local-first Gateway (~100MB Node.js) | **Heavyweight Runtime** — Manual (~50MB TS) | **Heavyweight Runtime** — Docker per session (~2GB container) | **Heavyweight Runtime** — Manual; spawns CLI subprocesses per agent | **Heavyweight Runtime** — **OS-native daemon** (LaunchAgent / systemd / Task Scheduler) + crash recovery + exponential backoff | **Compact Binary** — Manual; Rust binary | **Compact Binary** — Manual; Rust binary | **Compact Binary** — Manual; ~6 MB compact binary | **Compact Binary** — Ultra-lightweight (<10MB RAM, $10 hardware RISC-V/ARM) + Android APK | **Compact Binary** — **Always-on Telegram bot** (deployed wherever it can hold a persistent connection) | **Serverless Hibernation** — **Serverless hibernation** (Daytona/Modal) + Docker / SSH / Singularity | **Heavyweight Runtime** — Manual | **Embedded Bare-Metal** — **Bare-metal ESP32** (pure C, dual-core partitioning) | **Embedded Bare-Metal** — **Bare-metal embedded** (OSAL FreeRTOS/RT-Thread/Linux) | **Embedded Bare-Metal** — **Bare-metal Zig binary** (~450KB) |
| Resilience | **Append-only log + SQLite-backed queue + transactional tool-result commit (UUID per call) + mid-turn crash detection on startup + trigger catch-up (`was_due_at`) + channel reconnection with `last_event_id` replay**; fail-open Hooks; tool-aware compaction; loop guard; LiteLLM Router failover; atomic workspace writes | Local-first Gateway with persistent channel state | **SQLite-backed queue** + **`host-sweep.ts`** (60s sweep: stale-session detection, due-message wake, recurrence) + **`inbound.db` / `outbound.db` separation** with one writer per file (idempotent message flow, no cross-mount contention) | Docker per session — crashed session does not take down others | **SQLite WAL queue** per-agent workspace (each agent state independent) | **OS-daemon supervision** (LaunchAgent / systemd / Task Scheduler) + crash recovery + exponential backoff | WASM sandboxing limits crash blast radius; PostgreSQL durability | Reflector dedupe handles duplicate facts after partial recovery | **SHA256 loop detection** + **tiered compaction (70/90/95%)** + **hot-config reload** | Hooks fire on loop events (recovery hooks attachable); rule-based routing for task-level failover | Telegram channel persistence; chat_id-keyed state recovery | **6 backends** (local / Docker / SSH / Daytona / Modal / Singularity) — partial failure ≠ total failure | Workspace + SQLite (small core); standard Python resilience | **`HEARTBEAT.md` self-edits in plain text** — survives any reboot by construction; ESP32 reboot-tolerant | **Embedded swarm with capability bitmap** — node failover | **Priority-based context truncation** (graceful degradation) |
